<div class="container">
  <div class="row">
    <main class="col-8 offset-2">

      <h3>新規登録</h2>
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <!--エラー文の表示-->
        <%= render "devise/shared/error_messages", resource: resource %>

        <div class="field form-group mt-3">
          <%= f.label :ユーザー名 %><br />
          <%= f.text_field :name, autofocus: true, class:"form-control col-10" %>
        </div>

        <div class="field form-group">
          <%= f.label :Eメール %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"form-control col-10" %>
        </div>

        <div class="field form-group">
          <%= f.label :パスワード %>
          <% if @minimum_password_length %>
          <em>(<%= @minimum_password_length %> 文字以上)</em>
          <% end %><br />
          <%= f.password_field :password, autocomplete: "new-password", class:"form-control col-10" %>
        </div>

        <div class="field form-group">
          <%= f.label :パスワード（確認） %><br />
          <%= f.password_field :password_confirmation, autocomplete: "new-password", class:"form-control col-10" %>
        </div>

        <div class="actions form-group">
          <%= f.submit "新規登録", class:"btn btn-md btn-success" %>
        </div>
      <% end %>

    </main>
  </div>
</div>