<% if item.liked_by?(current_user) %>
  いいね！<%= link_to item.likes.count, item_likes_path(item), method: :delete, remote: true, class:"fas fa-thumbs-up" %>
<% else %>
  いいね！<%= link_to item.likes.count, item_likes_path(item), method: :post, remote: true, class:"far fa-thumbs-up" %>
<% end %>